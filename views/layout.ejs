<!doctype html>
<html>

<head>
    <title>
        <%- title %>
    </title>
    <link rel="stylesheet" href="/bootstrap.css" type="text/css" />
    <link rel="stylesheet" href="/mocha.css" type="text/css" />
    <link rel="icon" type="image/png" href="/csv.png" />
    <script type="text/javascript" src="/underscore-min.js"></script>
    <script type="text/javascript" src="/jquery-2.1.3.min.js"></script>
    <script type="text/javascript" src="/bootstrap.min.js"></script>
    <script>
        $(document).ready(function(){
            //check storage data
            if (window.localStorage && localStorage.csv_text) {
                $("#csv_text").val(localStorage.csv_text);
            }
            $("#analizar").click(function(event){
                event.preventDefault();
                $.get("/csv", {csv_text: csv_text.value}, function( data ) {
                    $( "#tabla" ).text( data);
                    console.log(data);
                });
                //storage data
                if(window.localStorage)
                    localStorage.csv_text = $("#csv_text").val();
            });
        });
    </script>
    
</head>

<body>
    <%- body %>
</body>

</html>